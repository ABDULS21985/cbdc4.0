apiVersion: v1
kind: Secret
metadata:
  name: cbdc-secrets
  namespace: cbdc-platform
type: Opaque
stringData:
  # Database Credentials (base64 encode in production)
  DB_USER: "cbdc_user"
  DB_PASSWORD: "CHANGE_ME_IN_PRODUCTION"

  # JWT Secret for API Authentication
  JWT_SECRET: "CHANGE_ME_IN_PRODUCTION"

  # Fabric Identity Credentials
  FABRIC_USER: "Admin"
  FABRIC_MSP_ID: "Org1MSP"

  # API Keys
  CBN_API_KEY: "CHANGE_ME_IN_PRODUCTION"
  RTGS_API_KEY: "CHANGE_ME_IN_PRODUCTION"
---
apiVersion: v1
kind: Secret
metadata:
  name: fabric-certs
  namespace: cbdc-platform
type: Opaque
data:
  # Placeholder - mount actual certificates from Fabric CA
  # cert.pem: <base64-encoded-cert>
  # key.pem: <base64-encoded-key>
  placeholder: cGxhY2Vob2xkZXI=
